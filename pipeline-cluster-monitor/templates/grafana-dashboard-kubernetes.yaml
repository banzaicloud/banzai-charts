apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fullname" . }}-grafana-dashboard-kubernetes
  labels:
    pipeline_grafana_dashboard: "1"
data:
  kubernetes.json: |-2

{{.Files.Get "grafana-dashboards/kuberntes.json"| indent 4}}