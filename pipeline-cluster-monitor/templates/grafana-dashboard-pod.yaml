apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fullname" . }}-grafana-dashboard-pod
  labels:
    pipeline_grafana_dashboard: "1"
data:
  kubernetes.json: |-2

{{.Files.Get "grafana-dashboards/pod.json"| indent 4}}