apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fullname" . }}-grafana-dashboard-deployment
  labels:
    pipeline_grafana_dashboard: "1"
data:
  kubernetes.json: |-
{{.Files.Get "grafana-dashboards/deployment.json"| indent 4}}