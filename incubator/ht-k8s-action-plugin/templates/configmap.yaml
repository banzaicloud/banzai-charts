apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ template "htActionPlugin.fullname" . }}"
  labels:
    app: "{{ template "htActionPlugin.fullname" . }}"
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data:
  plugin-config.toml: |-
    [log]
        format = "{{ .Values.htActionPlugin.logFormat }}"
        level = "{{ .Values.htActionPlugin.logLevel }}"

    [plugin]
        port = "{{ .Values.htActionPlugin.bindAddr }}"
