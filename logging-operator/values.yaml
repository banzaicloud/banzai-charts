operator:
  namespace: default
  image:
    repository: banzaicloud/logging-operator
    tag: latest
    pullPolicy: Always

fluentbit:
  enabled: true
  namespace: default

fluentd:
  enabled: true
  namespace: default

tls:
  enabled: false
  secretName: ""

rbac:
  enabled: true

